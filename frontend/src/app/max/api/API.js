import {HOST} from './host';

const USER = `${HOST}/user`;
const AUTH = `${HOST}/auth`;
const ACTIVATION = `${HOST}/activate`;

const API = {
    USER,
    AUTH,
    ACTIVATION
};

export default API;
